Due to time constraints we were not able to provide fully functional unit testing,
but here we provide the testing scheme that should be implmenented on the code.

Testing:
Testing the pencil class:
#include <gtest/gtest.h>

TEST(pencil_class, make_pencil){
    pencil p;
    ASSERT_EQ(0, make_pencil);
    p.make_pencil();
    ASSERT_EQ(1, make_pencil);

    p.increasePrice();
    ASSERT_EQ(1, p.pencil_price);
    .
    .
    .
    //checking pencil_count
    //checking prices
    //checking rates
    //checking wood_coutn
    //checking graphite_count
    ...etc.
}

//the main 

int main(int argc, char* argv[]){
    testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}

